# public/img

The site's own artwork. Anything here is served from `/img/…`.

`logo.png` is the mark in the top bar. It is displayed 34 px tall, so give it a
transparent background and a couple of hundred pixels of height to stay sharp on
a retina screen. Until the file exists the bar falls back to a plain pin.
